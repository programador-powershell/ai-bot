{
  "name": "@aibot2/server",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "O servidor do AI-BOT 2: o chassis HTTP forkado do openbot (Hono + Bun.serve, auth/policy/audit/rotas — MIT, ver THIRD_PARTY_NOTICES.md) FUNDIDO com a nossa montagem do harness-kernel (src/montagem). O db relacional do chassis é drizzle + bun:sqlite (chassis.db); o event log das conversas continua no StorageDriver (@aibot2/domain-events) — duas verdades que não se misturam (integracao-openbot.md §4.4).",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "db:generate": "bun x drizzle-kit generate --config=drizzle.config.ts",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@aibot2/domain-events": "workspace:*",
    "@aibot2/harness-kernel": "workspace:*",
    "@aibot2/harness-openbot-bridge": "workspace:*",
    "@ag-ui/client": "0.0.57",
    "@better-auth/drizzle-adapter": "^1.6.27",
    "@copilotkit/runtime": "1.67.1",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "better-auth": "^1.6.27",
    "cel-js": "^0.8.2",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.10.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@copilotkit/aimock": "^1.38.0",
    "@types/bun": "^1.3.3",
    "drizzle-kit": "^0.31.10",
    "eventsource": "3.0.7"
  }
}
